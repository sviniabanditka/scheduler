# Laravel Scheduler - Docker Environment

Этот проект представляет собой Laravel приложение с настроенным Docker окружением для разработки.

## 🐳 Docker Services

- **Laravel App** (PHP 8.2-fpm) - основное приложение
- **Nginx** - веб-сервер
- **MySQL 8.0** - база данных
- **Redis** - кэш и сессии

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd scheduler

# Скопируйте файл окружения
cp docker.env.example .env

# Сгенерируйте ключ приложения
make install
```

### 2. Запуск проекта

```bash
# Соберите и запустите контейнеры
make build
make up

# Выполните миграции
make migrate

# (Опционально) Заполните базу тестовыми данными
make seed
```

### 3. Доступ к приложению

- **Веб-приложение**: http://localhost:8080
- **MySQL**: localhost:3306
- **Redis**: localhost:6379

## 🛠️ Доступные команды

| Команда | Описание |
|---------|----------|
| `make build` | Собрать Docker контейнеры |
| `make up` | Запустить все сервисы |
| `make down` | Остановить все сервисы |
| `make restart` | Перезапустить все сервисы |
| `make install` | Установить зависимости Laravel |
| `make migrate` | Выполнить миграции базы данных |
| `make seed` | Заполнить базу тестовыми данными |
| `make fresh` | Сбросить и пересоздать базу с тестовыми данными |
| `make logs` | Показать логи всех сервисов |
| `make logs-app` | Показать логи приложения |
| `make logs-nginx` | Показать логи Nginx |
| `make logs-db` | Показать логи базы данных |
| `make shell` | Войти в контейнер приложения |
| `make shell-db` | Войти в контейнер базы данных |
| `make test` | Запустить тесты |
| `make cache-clear` | Очистить кэш приложения |
| `make clean` | Очистить контейнеры и тома |

## 📁 Структура проекта

```
scheduler/
├── app/                    # Код приложения Laravel
├── config/                 # Конфигурационные файлы
├── database/               # Миграции, сидеры, фабрики
├── docker/                 # Docker конфигурации
│   ├── nginx/             # Конфигурация Nginx
│   ├── php/               # Dockerfile и конфигурация PHP
│   └── mysql/             # Конфигурация MySQL
├── public/                 # Публичные файлы
├── resources/              # Ресурсы (views, assets)
├── routes/                 # Маршруты
├── storage/                # Файлы хранилища
├── tests/                  # Тесты
├── docker-compose.yml      # Docker Compose конфигурация
├── Makefile               # Команды для управления
└── README.md              # Документация
```

## 🔧 Настройка окружения

### Переменные окружения

Основные переменные в `.env`:

```env
APP_NAME=Scheduler
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=scheduler
DB_USERNAME=scheduler
DB_PASSWORD=scheduler_password

REDIS_HOST=redis
REDIS_PORT=6379
```

### Автоперезагрузка

Проект настроен на автоперезагрузку при изменении кода:
- Volumes подключены для синхронизации файлов
- Xdebug настроен для отладки
- Nginx автоматически подхватывает изменения

## 🐛 Отладка

### Xdebug

Xdebug настроен для отладки. Для подключения:

1. Установите расширение Xdebug в вашей IDE
2. Настройте подключение к `localhost:9003`
3. Установите breakpoints в коде

### Логи

```bash
# Просмотр логов приложения
make logs-app

# Просмотр логов Nginx
make logs-nginx

# Просмотр логов базы данных
make logs-db
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
make test

# Вход в контейнер для ручного тестирования
make shell
```

## 📝 Разработка

### Добавление новых зависимостей

```bash
# Войти в контейнер
make shell

# Установить пакет через Composer
composer require package/name

# Или использовать Docker Compose напрямую
docker-compose exec app composer require package/name
```

### Работа с базой данных

```bash
# Выполнить миграции
make migrate

# Создать новую миграцию
make shell
php artisan make:migration create_table_name

# Создать новый сидер
php artisan make:seeder TableNameSeeder
```

## 🚨 Устранение неполадок

### Проблемы с правами доступа

```bash
# Исправить права доступа
sudo chown -R $USER:$USER .
chmod -R 755 storage bootstrap/cache
```

### Очистка окружения

```bash
# Полная очистка
make clean
make build
make up
```

### Проблемы с портами

Если порты заняты, измените их в `docker-compose.yml`:

```yaml
ports:
  - "8081:80"  # Изменить 8080 на 8081
```

## 📚 Дополнительные ресурсы

- [Laravel Documentation](https://laravel.com/docs)
- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
